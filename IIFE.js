// Immediately Invoked Function Expressions (IIFE)

/* 
  They are typically used to create a local scope for variables to 
  prevent them from polluting the global scope.
*/

(function chai(){
    // named IIFE
    console.log(`DB CONNECTED`);
})(); //semicolon is necessary

